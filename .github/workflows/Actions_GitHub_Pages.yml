name: Actions GitHub Pages
on:
  push:
    branches: [main]
  schedule:
    - cron: "0 16 * * 0"
  workflow_dispatch:
    

jobs:
  del_Actions:
    uses: 3wlh/Actions/.github/workflows/Call_Actions.yml@main
    secrets: inherit